# T001 — Create workspace shell

## Objective
Scaffold the monorepo with web app, server app, shared package, db package, and rolepack package.

## Acceptance criteria
- `pnpm install` succeeds
- `pnpm dev` boots server and web
- `GET /api/health` returns 200
