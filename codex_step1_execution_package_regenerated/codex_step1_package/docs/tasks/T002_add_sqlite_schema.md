# T002 — Add SQLite schema

## Objective
Implement DB package with schema, migration runner, and seed loader using the provided SQL and JSON contracts.

## Acceptance criteria
- DB file is created
- tables exist
- seed inserts without errors
