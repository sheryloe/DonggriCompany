# T003 — Implement bootstrap API

## Objective
Build health, bootstrap state/init, provider listing, provider probe, and rolepack list endpoints.

## Acceptance criteria
- endpoints match contract shape
- bootstrap init persists state
- errors are consistent
