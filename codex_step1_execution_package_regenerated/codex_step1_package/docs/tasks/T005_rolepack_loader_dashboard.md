# T005 — Add rolepack loader and dashboard shell

## Objective
Load role packs from disk and render a minimal employee-first dashboard shell.

## Acceptance criteria
- `/api/rolepacks` returns discovered packs
- dashboard renders employees and basic inspector shell
