services:
  donggricompany:
    environment:
      # V1 executes provider CLIs through the Windows host-native Supervisor.
      # Never grant the application access to the host Docker daemon socket.
      OFFICE_RUNNER_DOCKER_ENABLED: "0"
