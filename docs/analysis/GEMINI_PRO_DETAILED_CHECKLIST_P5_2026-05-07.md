<!--
Generated by Gemini CLI for Codex implementation planning.
TrustedWorkspaceEnv: GEMINI_CLI_TRUST_WORKSPACE=true
PrimaryCommand: gemini -m gemini-3.1-pro-preview --approval-mode plan -p <prompt>
PrimaryCapacity429Observed: True
FallbackCommand: gemini -m gemini-2.5-pro --approval-mode plan -p <prompt>
FallbackPolicy: scripts/run-gemini-pro-analysis.ps1 falls back when the primary model exits nonzero, emits empty output, times out, or reports 429/capacity.
ReportBodySource: gemini-3.1-pro-preview stdout captured before fallback timeout.
GeneratedAt: 2026-05-07 Asia/Seoul
-->

# Gemini Pro Detailed Analysis and Codex Checklist - 2026-05-07

## 1. Executive Summary

- **P0-P4 현재 상태 점수:** **85 / 100**
- 전반적인 시스템 컴포넌트(부서별 뷰, 의사결정 모달, 오피스 라우팅, 작업 카드 등)의 UI/UX 및 기초 API 연동은 상당히 완성도가 높으나(특히 TaskCard의 observability 및 DecisionInbox의 다중 선택/리스크 분석), 일부 핵심 폴백(Fallback) 및 검색 메커니즘에서 고도화가 필요합니다.
- **운영 완성도에 영향을 미치는 상위 5가지 갭(Gaps):**
  1. **Visual Profile Reserve Fallback 로직 미비:** 현재 에이전트의 시각적 프로필이 명시되지 않았을 때 해시 알고리즘을 통한 단순 인덱스 할당만 이루어지고 있으며, `active` 프로필의 에셋 로드 실패 시 `reserve` 상태의 프로필로 전환되는 실질적인 방어 로직이 없습니다.
  2. **장기 기억(Long-term memory) 검색 최적화 부족:** `server/modules/memory/store.ts` 및 `beads-bridge.ts` 구조는 갖추어져 있으나, 문맥 기반의 시맨틱(Semantic) 검색 및 복합 필터링 기능이 프론트엔드/백엔드에 완벽히 연계되지 않았습니다.
  3. **디자인 부서 워크스페이스 특화 부족:** 부서(Department) 컴포넌트 뷰 구조는 존재하나, 디자인 부서에 특화된 캔버스 혹은 에셋 리뷰 전용 툴이 누락되어 있습니다.
  4. **프로젝트 스코프 컴포넌트 이벤트의 시각화 결여:** API(`getProjectComponentEvents`) 레벨의 연동은 있으나, 이를 타임라인 형태로 묶어서 실시간으로 보여주는 집계 UI/UX가 부족합니다.
  5. **의사결정 사후 결과(After-result) 추적 기능 미비:** `DecisionInboxModal`에서 결정 전(Rationale, Risk, Follow-up) 분석은 훌륭하나, 선택 후 실제 발생한 결과와 예상치를 대조(Choice modeling vs Result)하는 사후 피드백 루프가 아직 구현 전입니다.

## 2. Evidence Reviewed

- **검토된 소스 / 테스트 / 문서 영역:**
  - `src/agent-visual-profiles.ts` (시각적 프로필 및 폴백 로직 확인)
  - `src/components/DepartmentComponentsView.tsx` (부서별 컴포넌트 및 엔트리 포인트 맵핑 확인)
  - `src/components/DecisionInboxModal.tsx` (의사결정 Inbox 및 Analysis Panel 검토)
  - `src/components/taskboard/TaskCard.tsx` (작업 카드 및 Live Ops 지표 연동 구조 확인)
  - `server/modules/memory/` (`store.ts`, `beads-bridge.ts` 확인)
  - `src/components/office-view/` (오피스 네비게이션 및 씬 빌드 스크립트 존재 확인)
- **검증 불가/제한된 영역:**
  - 백엔드 DB(Database) 스키마와 인덱싱 상태 (인프라 구성 요소 및 비밀 키(secrets)를 포함할 수 있어 조회 제한됨).
  - 프로덕션 환경의 라이브 웹소켓 트래픽(Live Ops observability의 실제 스트리밍 성능).

## 3. Feature Completeness Review

| Area                                 | Current State                                                       | Gap                                                                      | Risk                 | Priority |
| :----------------------------------- | :------------------------------------------------------------------ | :----------------------------------------------------------------------- | :------------------- | :------- |
| **Visual profile reserve fallback**  | 상태값(`reserve`) 정의는 있으나 해시 기반의 정적 프리셋 제공에 그침 | `active` 에셋 로드 실패 시 `reserve` 에셋으로 동적 렌더링 전환 로직 부재 | Medium (UI 깨짐)     | P0       |
| **Long-term memory search**          | Store 및 Beads 브릿지 존재                                          | 고급 검색(시맨틱/복합 필터링) 기능 미흡                                  | High (컨텍스트 유실) | P1       |
| **Design department workspace**      | 기본 Department 배열에 포함됨                                       | 디자이너 특화 툴(Asset review/Canvas) UI 부재                            | Low                  | P2       |
| **Project-scoped component history** | `getProjectComponentEvents` API 구현됨                              | 타임라인 기반의 통합 히스토리 대시보드 부재                              | Medium (추적 불가)   | P2       |
| **Decision analysis 1/2 choice**     | `DecisionOptionAnalysisPanel` 등 분석 UI 완성                       | 선택 후 발생한 결과와 예측을 대조하는 사후 분석 추적 부재                | Low                  | P3       |
| **Office room to dept navigation**   | `entryPoints: ["office_room"]` 연동 완료                            | 시각적 씬 트랜지션 애니메이션 고도화 필요                                | Low                  | P4       |
| **Task cards / Live Ops**            | Subtask, CEO Call, Routing Meta 등 완벽                             | 부서간 실시간 통계 차트 컴포넌트 미비                                    | Low                  | P4       |
| **Tests and build coverage**         | 단위 테스트 파일 다수 존재 (ex. `DecisionInboxModal.test.tsx`)      | Fallback 로직 및 메모리 Store 관련 엣지 케이스 커버리지 부족             | Medium               | P1       |

## 4. Codex Implementation Checklist

### [P0/P1] Immediate & High Impact

- **ID: CODEX-P0-01 (Visual Profile Reserve Fallback)**
  - **Required Code Change:** `src/agent-visual-profiles.ts`의 `resolveAgentVisualProfile` 함수를 수정하여, agent의 프로필 상태를 평가하고 에셋 로드 실패 핸들러와 연동될 수 있는 명시적 `reserve` 프로필 반환 로직을 추가하세요.
  - **Acceptance Criteria:** 유효하지 않은 `visual_profile_key`가 주어졌을 때, 단순히 해시를 돌리는 대신 백업용(`reserve` status) 프로필 목록에서 적절히 할당해야 합니다.
  - **Recommended Validation Command:** `pnpm vitest src/agent-visual-profiles.test.ts`
  - **Risk if skipped:** 에이전트 이미지 에러 발생 시 UI 신뢰성 저하.

- **ID: CODEX-P1-01 (Long-term Memory Search API Refinement)**
  - **Required Code Change:** `server/modules/memory/store.ts`의 검색 쿼리를 확장하여 태그 기반 필터링 및 날짜 범위를 지원하는 `searchMemory(query, filters)` 함수를 구현하세요.
  - **Acceptance Criteria:** 키워드와 메타데이터 속성으로 복합 검색이 가능해야 하며, 해당 기능이 `beads-bridge.ts`를 통해 노출되어야 합니다.
  - **Recommended Validation Command:** `pnpm vitest server/modules/memory/`
  - **Risk if skipped:** 에이전트가 과거의 중요 의사결정을 잊어버리는 현상 발생.

### [P2] Secondary Improvements

- **ID: CODEX-P2-01 (Project Component Event Timeline)**
  - **Required Code Change:** `src/components/DepartmentComponentsView.tsx` 내에 `ProjectComponentEvent` 배열을 렌더링하는 전용 Timeline UI 컴포넌트를 추가하세요.
  - **Acceptance Criteria:** 각 이벤트가 시간 역순으로 정렬되며, 이벤트의 `metrics`에 따른 변화량이 시각적으로 표시되어야 합니다.
  - **Recommended Validation Command:** `pnpm vitest src/components/DepartmentComponentsView.test.tsx`
  - **Risk if skipped:** 프로젝트 병목의 원인 파악 지연.

- **ID: CODEX-P2-02 (Design Department Workspace Stub)**
  - **Required Code Change:** `COMPONENTS` 배열에 Design 부서를 위한 특화 ComponentKind(예: `asset-canvas`)를 정의하고 목업을 연결하세요.
  - **Acceptance Criteria:** Design 부서 탭 선택 시 기본 칸반이 아닌 에셋 리뷰 특화 UI가 로드되어야 합니다.
  - **Recommended Validation Command:** `pnpm run build` (타입 검증)
  - **Risk if skipped:** 디자인 부서 전용 워크플로우 활성화 불가.

### [P3/P4] Enhancements

- **ID: CODEX-P3-01 (Decision After-result Tracking)**
  - **Required Code Change:** `src/components/DecisionInboxModal.tsx`의 옵션 타입에 `actualResult` 필드를 추가하고, 완료된 결정에 대해 이를 표시하는 UI를 추가하세요.
  - **Acceptance Criteria:** 완료된 결정(Archive) 조회 시, 예상 결과(Expected Result) 옆에 실제 결과(Actual Result)가 비교 렌더링되어야 합니다.
  - **Recommended Validation Command:** `pnpm vitest src/components/DecisionInboxModal.test.tsx`
  - **Risk if skipped:** 장기적인 AI 의사결정 모델의 정확도 개선 지연.

## 5. Suggested Immediate Patch Set

Codex가 가장 먼저 구현해야 할 핵심 패치셋은 **Visual Profile Reserve Fallback 로직 개선 (CODEX-P0-01)**입니다.

- **이유:** 이 패치는 클라이언트 단일 파일(`src/agent-visual-profiles.ts`)의 순수 함수 로직을 개선하는 것으로, DB나 백엔드 인프라 수정 없이 즉각적으로 UI의 안정성을 향상시킵니다.
- **패치 범위:** `resolveAgentVisualProfile` 내부의 하드코딩된 해시 분기 처리를 고도화하고, `status === "reserve"` 인 프로필들을 필터링하는 헬퍼 함수를 추가합니다.

## 6. Test Plan

1. **단위 테스트 (우선 수행):**
   - `pnpm vitest src/agent-visual-profiles.test.ts` (폴백 로직 검증, 존재하지 않는 프로필 키 입력 시 예비 프로필 반환 확인)
   - `pnpm vitest server/modules/memory/` (메모리 스토어 검색 쿼리 파라미터 검증)
2. **컴포넌트 테스트:**
   - `pnpm vitest src/components/DepartmentComponentsView.test.tsx` (Timeline 렌더링 무결성 확인)
   - `pnpm vitest src/components/DecisionInboxModal.test.tsx` (Decision 옵션 UI 렌더링 확인)
3. **통합 빌드 및 타입 검증:**
   - `pnpm run build` 및 `pnpm tsc --noEmit` 실행하여 타입 오류 부재 확인.
4. **브라우저 스크린샷 검증 (선택사항):**
   - 디자인 워크스페이스 및 TaskCard 컴포넌트에 대한 Storybook 또는 Playwright 렌더링 스냅샷 확인.

## 7. Remaining Risks

- 이번 즉각적인 패치셋 적용 이후에도 백엔드의 **메모리(Memory) 검색 성능 확장성 문제**가 남아있을 수 있습니다. 시스템이 오래 실행될수록 스토어에 데이터가 쌓여 단순 쿼리로는 부하가 발생할 수 있습니다.
- 부서간 라우팅(`Office room to dept navigation`) 성능: 다수의 3D/2D 씬 컴포넌트가 로드되면서 WebGL/PixiJS 런타임 메모리 누수가 없는지에 대한 장기 모니터링이 필요합니다.
