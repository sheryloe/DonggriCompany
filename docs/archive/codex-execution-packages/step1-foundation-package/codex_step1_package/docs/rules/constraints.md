# Constraints

## Non-negotiable rules
1. Employee-first model
2. OAuth boundary belongs to provider CLIs
3. Local-first, single-user
4. No Telegram in Step 1
5. SQLite is source of truth
6. Build shell and placeholders only
7. Probe providers only: installed/config path/login signal
8. Jules is remote-async in later steps

## Code quality rules
- TypeScript strict mode
- stable JSON responses
- central error format
