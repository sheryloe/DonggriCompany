# Office control deck backup

This file records that the previous experimental `OfficeView.tsx` implementation was a control-deck dashboard.
The default Office view is now a visual office map again because the product surface must show an actual office.

Restore policy:

- Office default: visual office map.
- Control deck: future secondary tab only, not the default office screen.
- UI display text: Korean only.
- Internal/API keys: English canonical.
