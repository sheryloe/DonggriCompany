# Google OAuth Validation

- Callback path is canonical.
- Required secret slots are marked configured or missing.
- No raw token, client secret, refresh token, or authorization code is written to the artifact delta.
