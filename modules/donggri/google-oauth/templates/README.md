# Google OAuth Template Notes

This directory is intentionally metadata-only in v1.

Donggri writes a `.donggri/modules/<binding_name>.json` binding manifest and expects the project implementation layer to consume it.
