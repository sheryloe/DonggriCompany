# Landscape Image Template Notes

Use this module when a project needs reusable background or environment image generation.

The module does not call image generation itself; it prepares the prompt and review contract.
