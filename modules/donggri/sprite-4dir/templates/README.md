# 4-Direction Sprite Template Notes

Use this module for game characters that need directional movement poses.

The module is separate from agent avatar sprite registration and does not use `/api/sprites/process`.
