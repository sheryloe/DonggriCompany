import type { ProviderKey } from "@workspace/shared";

export const WORKSPACE_ID = "workspace_local_main";
export const BOOTSTRAP_STATE_ID = "bootstrap_state_local_main";

export const PROVIDERS: ProviderKey[] = ["claude", "codex", "gemini", "jules"];
